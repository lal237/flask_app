apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flask_app_chart.fullname" . }}-test-connection"
  labels:
    {{- include "flask_app_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flask_app_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
